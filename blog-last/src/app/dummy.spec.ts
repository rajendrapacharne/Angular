describe('dummy file', () => {
    it("should check if the amount is equal", () => {
        expect(100).toBe(100);
    });

    it("should check if the amount is equal", () => {
        expect(100).toBe(100);
    });
});
